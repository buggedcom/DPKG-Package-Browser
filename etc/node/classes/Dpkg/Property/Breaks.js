import _HasPackageVersion from './_HasPackageVersion';

/**
 * Class Breaks
 *
 * Holds the package "Breaks" property.
 * The value is an array of PackageVersion instances.
 *
 * @author Oliver Lillie
 */
class Breaks extends _HasPackageVersion {
}

module.exports = Breaks;
